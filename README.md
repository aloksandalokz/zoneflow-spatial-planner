# Alok VP Isolate for 3ds Max 2026

Viewport-only isolation tool. Selected objects can remain visible in one viewport while other viewports continue showing the full scene. Object renderability is never changed.

Build workflow compiles the native bridge on GitHub Actions so the workstation does not need admin rights, the 3ds Max SDK, Visual Studio, or CMake.
